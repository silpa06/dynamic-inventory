- name: install nginx
  hosts: all
  become: yes
  vars_files:
   - /var/ansible-cred/cred.yaml

  tasks:
  - name: nginx installation
    package:
      name: nginx
      state: present

  - name: start and enable nginx
    service:
      name: nginx
      state: started
      enabled: yes
